version: "2"
run:
  modules-download-mode: readonly
  tests: true
linters:
  default: all
  enable:
    - fatcontext
    - gomodguard_v2
    - nosprintfhostport
    - perfsprint
    - sloglint
    - thelper
    - usetesting
    - wastedassign
  disable:
    - gochecknoglobals
    - gomodguard
    - nestif
    - wrapcheck
    - wsl
  settings:
    cyclop:
      max-complexity: 15
    depguard:
      rules:
        main:
          list-mode: lax
          files:
            - main.go
            - cli.go
            - mcp.go
            - testhelpers.go
          allow:
            - $gostd
            - "github.com/modelcontextprotocol/go-sdk/mcp"
            - "searxng-mcp-go/internal/searxng"
          deny:
            - pkg: "net/http"
              desc: "Use internal client wrapper instead"
    dupl:
      threshold: 100
    errcheck:
      check-type-assertions: true
      check-blank: true
    errorlint:
      asserts: true
      comparison: true
    gocognit:
      min-complexity: 20
    goconst:
      min-len: 2
      min-occurrences: 2
    gocritic:
      enabled-checks:
        - deferInLoop
        - httpNoBody
        - unnecessaryDefer
    gocyclo:
      min-complexity: 13
    govet:
      enable:
        - shadow
    interfacebloat:
      max: 5
    lll:
      line-length: 120
    misspell:
      locale: US
    nakedret:
      max-func-lines: 1
    funlen:
      lines: 60
      statements: 40
    prealloc:
      simple: true
      range-loops: true
    sloglint:
      args-order: true
      keys-only: true
    varnamelen:
      min-name-length: 2
    nolintlint:
      require-specific: true
      require-explanation: true
  exclusions:
    generated: lax
    paths:
      - .*\.pb\.go$
      - third_party$
      - builtin$
      - examples$
    rules:
      - linters: [exhaustruct]
        paths:
          - ".*_test\\.go$"
        text: ".*"
      - linters: [forbidigo]
        paths:
          - "cli\\.go$"
        text: ".*"
      - linters: [funlen]
        paths:
          - ".*_test\\.go$"
        text: ".*"
      - linters: [dupl]
        paths:
          - ".*_test\\.go$"
        text: ".*"
      - linters: [gosmopolitan]
        paths:
          - ".*_test\\.go$"
        text: ".*"
      - linters: [tagliatelle]
        paths:
          - "internal/searxng/types\\.go$"
        text: "json\\(camel\\): got '(time_range|feels_like|wind_from|wind_speed|cloud_cover|country_code|number_of_results|unresponsive_engines)'"
      - linters: [paralleltest]
        paths:
          - "error_path_test\\.go$"
        text: ".*"
      - linters: [paralleltest]
        paths:
          - "golden_capture_test\\.go$"
        text: ".*"
      - linters: [revive]
        paths:
          - "internal/searxng/(errors|searcher)\\.go$"
        text: "exported: type name will be used as searxng\\.(SearXNGError|SearXNGSearcher)"
      - linters: [goconst]
        paths:
          - ".*_test\\.go$"
        text: ".*"
      - linters: [varnamelen]
        paths:
          - ".*_test\\.go$"
        text: ".*"

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
  settings:
    gci:
      sections:
        - standard
        - default
        - prefix(searxng-mcp-go)
    gofumpt:
      extra-rules: true
      module-path: searxng-mcp-go
  exclusions:
    generated: lax
    paths:
      - .*\.pb\.go$
      - third_party$
      - builtin$
      - examples$
